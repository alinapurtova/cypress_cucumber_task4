{
  "name": "cypress_cucumber_task4",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "cypress:open": "npx cypress open",
    "cypress:run": "npx cypress run",
    "test": "cypress run",
    "merge-reports": "npx mochawesome-merge cypress/reports/html/*.json > cypress/reports/html/report.json",
    "generate-report": "npx marge cypress/reports/html/report.json --reportDir cypress/reports/html --inline",
    "test:report": "npm run test && npm run merge-reports && npm run generate-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^23.2.1",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.6",
    "@faker-js/faker": "^10.1.0",
    "cypress": "^15.5.0",
    "esbuild": "^0.19.0",
    "mochawesome": "^7.1.4",
    "mochawesome-report-generator": "^6.3.0",
    "mochawesome-merge": "^5.0.0"
  },
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": false,
    "stepDefinitions": "cypress/e2e/step_definitions/**/*.{js,ts}"
  }
}